# Инструкция по установке и запуску CoinCortex

## Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/ваш-username/CoinCortex.git
cd CoinCortex/CoinCortex
```

## Шаг 2: Создание виртуального окружения

### Windows:
```bash
python -m venv venv
venv\Scripts\activate
```

### Linux/Mac:
```bash
python3 -m venv venv
source venv/bin/activate
```

## Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

## Шаг 4: Настройка базы данных

```bash
python manage.py migrate
```

## Шаг 5: Создание суперпользователя (опционально)

```bash
python manage.py createsuperuser
```

Введите имя пользователя, email и пароль для доступа к админ-панели Django.

## Шаг 6: Запуск сервера

```bash
python manage.py runserver
```

## Шаг 7: Открытие в браузере

Перейдите по адресу: **http://127.0.0.1:8000/**

## 🔐 Настройка безопасности (рекомендуется)

1. Создайте файл `.env` в папке `CoinCortex/`:
```env
SECRET_KEY=ваш-секретный-ключ-здесь
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
```

2. Установите `python-decouple`:
```bash
pip install python-decouple
```

3. Обновите `settings.py` для использования переменных окружения.

## ❓ Решение проблем

### Ошибка: "No module named 'django'"
**Решение:** Убедитесь, что виртуальное окружение активировано и зависимости установлены:
```bash
pip install -r requirements.txt
```

### Ошибка: "ModuleNotFoundError: No module named 'django_bootstrap5'"
**Решение:** Установите недостающий пакет:
```bash
pip install django-bootstrap5
```

### Ошибка миграций
**Решение:** Удалите базу данных и примените миграции заново:
```bash
# Удалите db.sqlite3 (если есть)
python manage.py migrate
```

### Порт уже занят
**Решение:** Запустите на другом порту:
```bash
python manage.py runserver 8080
```

## 📝 Полезные команды

```bash
# Просмотр всех доступных команд
python manage.py help

# Создание миграций после изменения моделей
python manage.py makemigrations

# Открыть Django shell
python manage.py shell

# Создать суперпользователя
python manage.py createsuperuser
```

## 🚀 Готово!

Теперь проект должен работать. Зарегистрируйте нового пользователя или войдите через админ-панель.

